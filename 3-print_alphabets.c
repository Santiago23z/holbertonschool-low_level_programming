i#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - alafbet
 *
 * Return: Always 0 
 */
int main(void)
{
	char alf;

	for (alf = 'a'; alf <= 'z'; alf++)
		putchar(low);
	for (alf = 'A'; alf <= 'Z'; alf++)
		putchar(alf);
	putchar('\n');

	return (0);
}

#include <stdio.h>

/**
 * main - Prints th programs name, followed by a newline
 *@argc: count of arguments
 *@argv: array conter
 * Return: Always 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
